/*Find the smallest integer evenly divisible by all numbers less than and equal to 20.*/

#include <iostream>
#include <vector>

using namespace std;

int main(int argn, char* argv[]) {
	/*
		Decompose all the numbers between 1 and 20 into their primes.
		Identify the largest power used for each prime.
		Create a new number using each prime with its largest power.
	*/
	return 0;
}